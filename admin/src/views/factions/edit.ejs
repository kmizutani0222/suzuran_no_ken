<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title>管理 - 陣営編集</title>
  <link rel="stylesheet" href="/styles.css" />
</head>
<body>
  <div class="page-header">
    <h1>陣営編集</h1>
  </div>

  <div class="row">
    <div class="col s12">
      <div class="card">
        <div class="card-content">
          <form action="/factions/<%= item.id %>" method="post" enctype="multipart/form-data">
            <div class="row">
              <div class="input-field col s12">
                <input type="text" id="name" name="name" value="<%= item.name %>" required />
                <label for="name">名前</label>
              </div>
            </div>

            <div class="row">
              <div class="input-field col s12">
                <div class="file-field input-field">
                  <div class="btn blue">
                    <span>アイコン</span>
                    <input type="file" name="icon" accept="image/*" />
                  </div>
                  <div class="file-path-wrapper">
                    <input class="file-path validate" type="text" placeholder="画像を選択してください" />
                  </div>
                </div>
                <% if (item.icon) { %>
                  <div class="image-preview">
                    <p>現在のアイコン:</p>
                    <img src="<%= item.icon %>" alt="現在のアイコン" style="max-width: 100px; max-height: 100px;" />
                  </div>
                <% } %>
              </div>
            </div>

            <div class="row">
              <div class="col s12 text-center">
                <button type="submit" class="btn-large waves-effect waves-light blue">
                  <i class="material-icons left">update</i>更新
                </button>
              </div>
            </div>
          </form>

          <form action="/factions/<%= item.id %>/delete" method="post" style="margin-top:1rem;">
            <div class="row">
              <div class="col s12 text-center">
                <button type="submit" class="btn-large waves-effect waves-light red" onclick="return confirm('削除しますか？')">
                  <i class="material-icons left">delete</i>削除
                </button>
              </div>
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>
  <p><a href="/factions">一覧に戻る</a></p>
</body>
</html> 