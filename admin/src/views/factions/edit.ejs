<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title>管理 - 陣営編集</title>
</head>
<body>
  <h1>陣営編集</h1>
  <form action="/factions/<%= item.id %>" method="post">
    <div>
      <label>名前: <input type="text" name="name" value="<%= item.name %>" required /></label>
    </div>
    <div>
      <label>画像URL: <input type="text" name="image" value="<%= item.image %>" /></label>
    </div>
    <button type="submit">更新</button>
  </form>
  <form action="/factions/<%= item.id %>/delete" method="post" style="margin-top:1rem;">
    <button type="submit" onclick="return confirm('削除しますか？')">削除</button>
  </form>
  <p><a href="/factions">一覧に戻る</a></p>
</body>
</html> 