<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title>管理 - スキル一覧</title>
</head>
<body>
<div class="page-header">
  <h1>スキル一覧</h1>
</div>

<div class="row">
  <div class="col s12">
    <div class="card">
      <div class="card-content">
        <div class="card-title">
          <a href="/skills/new" class="btn-large waves-effect waves-light blue">
            <i class="material-icons left">add</i>新規登録
          </a>
        </div>
        
        <div class="table-container">
          <table class="striped responsive-table">
            <thead>
              <tr>
                <th>名前</th>
                <th>タイプ</th>
                <th>カテゴリー</th>
                <th>コスト</th>
                <th>CT</th>
                <th>操作</th>
              </tr>
            </thead>
            <tbody>
              <% if (skills.length === 0) { %>
                <tr>
                  <td colspan="6" class="text-center">データがありません</td>
                </tr>
              <% } %>
              <% skills.forEach(s => { %>
                <tr>
                  <td><%= s.name %></td>
                  <td><%= s.skillType %></td>
                  <td><%= s.skillCategory %></td>
                  <td><%= s.cost %></td>
                  <td><%= s.ct %></td>
                  <td>
                    <a href="/skills/<%= s.id %>/edit" class="btn-small waves-effect waves-light blue">
                      <i class="material-icons">edit</i>編集
                    </a>
                  </td>
                </tr>
              <% }) %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
</div>
</body>
</html> 