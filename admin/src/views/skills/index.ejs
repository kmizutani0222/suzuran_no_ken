<div class="page-header">
  <h1>スキル一覧</h1>
</div>

<div class="row">
  <div class="col s12">
    <div class="card">
      <div class="card-content">
        <table class="striped responsive-table">
          <thead>
            <tr>
              <th>アイコン</th>
              <th>名前</th>
              <th>CT</th>
              <th>コスト</th>
              <th>カテゴリー</th>
              <th>タイプ</th>
              <th>操作</th>
            </tr>
          </thead>
          <tbody>
            <% if (skills.length === 0) { %>
              <tr>
                <td colspan="7" class="text-center">データがありません</td>
              </tr>
            <% } %>
            <% skills.forEach(s => { %>
              <tr>
                <td>
                  <div class="character-images">
                    <% if (s.icon) { %>
                      <div class="image-preview">
                        <img src="<%= s.icon %>" alt="アイコン" title="アイコン" />
                      </div>
                    <% } else { %>
                      <div class="no-image">
                        <span class="grey-text">画像なし</span>
                      </div>
                    <% } %>
                  </div>
                </td>
                <td><%= s.name %></td>
                <td><%= s.ct %></td>
                <td><%= s.cost %></td>
                <td><%= s.skillCategory %></td>
                <td><%= s.skillType %></td>
                <td>
                  <a href="/skills/<%= s.id %>/edit" class="btn-small waves-effect waves-light orange">
                    <i class="material-icons">edit</i>
                  </a>
                  <form action="/skills/<%= s.id %>/delete" method="post" style="display:inline; margin-left: 10px;">
                    <button type="submit" class="btn-small waves-effect waves-light red" onclick="return confirm('削除しますか？')">
                      <i class="material-icons">delete</i>
                    </button>
                  </form>
                </td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>

<!-- 固定配置の新規登録ボタン -->
<a href="/skills/new" class="btn-floating fab-new waves-effect waves-light green">
  <i class="material-icons">add</i>
</a>