<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title>管理 - ロール編集</title>
</head>
<body>
  <h1>ロール編集</h1>
  <form action="/roles/<%= item.id %>" method="post">
    <div>
      <label>名前: <input type="text" name="name" value="<%= item.name %>" required /></label>
    </div>
    <div>
      <label>画像URL: <input type="text" name="image" value="<%= item.image %>" /></label>
    </div>
    <div>
      <label>移動力: <input type="number" name="movementPower" value="<%= item.movementPower %>" required min="0" /></label>
    </div>
    <div>
      <label>ジャンプ高: <input type="number" name="jumpHigh" value="<%= item.jumpHigh %>" required min="0" /></label>
    </div>
    <div>
      <label>ジャンプ低: <input type="number" name="jumpLow" value="<%= item.jumpLow %>" required min="0" /></label>
    </div>
    <div>
      <label>地形適正:
        <select name="terrainSuitability" required>
          <option value="normal" <%= item.terrainSuitability === 'normal' ? 'selected' : '' %>>通常</option>
        </select>
      </label>
    </div>
    <button type="submit">更新</button>
  </form>
  <form action="/roles/<%= item.id %>/delete" method="post" style="margin-top:1rem;">
    <button type="submit" onclick="return confirm('削除しますか？')">削除</button>
  </form>
  <p><a href="/roles">一覧に戻る</a></p>
</body>
</html> 