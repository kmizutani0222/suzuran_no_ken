<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title>管理 - キャラ一覧</title>
  <link rel="stylesheet" href="/styles.css" />
</head>
<body>
  <div class="page-header">
    <h1>キャラクター一覧</h1>
  </div>

  <div class="row">
    <div class="col s12">
      <div class="card">
        <div class="card-content">
          <div class="card-title">
            <a href="/characters/new" class="btn-large waves-effect waves-light blue">
              <i class="material-icons left">add</i>新規登録
            </a>
          </div>
          
          <div class="table-container">
            <table class="striped responsive-table">
              <thead>
                <tr>
                  <th>名前</th>
                  <th>武器種</th>
                  <th>ピクセルアバター</th>
                  <th>操作</th>
                </tr>
              </thead>
              <tbody>
                <% if (characters.length === 0) { %>
                  <tr>
                    <td colspan="4" class="text-center">データがありません</td>
                  </tr>
                <% } %>
                <% characters.forEach(c => { %>
                  <tr>
                    <td><%= c.name %></td>
                    <td><%= c.weaponType %></td>
                    <td>
                      <div class="character-images">
                        <% if (c.pixelAvatar) { %>
                          <div class="image-preview">
                            <img src="<%= c.pixelAvatar %>" alt="ピクセルアバター" title="ピクセルアバター" />
                          </div>
                        <% } else { %>
                          <div class="no-image">
                            <span class="grey-text">画像なし</span>
                          </div>
                        <% } %>
                      </div>
                    </td>
                    <td>
                      <a href="/characters/<%= c.id %>/edit" class="btn-small waves-effect waves-light blue">
                        <i class="material-icons">edit</i>編集
                      </a>
                    </td>
                  </tr>
                <% }) %>
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </div>
  </div>
</body>
</html> 