<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title>管理 - レアリティ一覧</title>
</head>
<body>
  <h1>レアリティ一覧</h1>
  <p><a href="/rarities/new">+ 新規登録</a></p>
  <table border="1" cellpadding="6">
    <thead>
      <tr>
        <th>名前</th>
        <th>画像</th>
        <th>値</th>
        <th>操作</th>
      </tr>
    </thead>
    <tbody>
      <% if (items.length === 0) { %>
        <tr><td colspan="4">データがありません</td></tr>
      <% } %>
      <% items.forEach(i => { %>
        <tr>
          <td><%= i.name %></td>
          <td><% if (i.image) { %><img src="<%= i.image %>" alt="" width="64" /><% } %></td>
          <td><%= i.value %></td>
          <td>
            <a href="/rarities/<%= i.id %>/edit">編集</a>
            <form action="/rarities/<%= i.id %>/delete" method="post" style="display:inline;">
              <button type="submit" onclick="return confirm('削除しますか？')">削除</button>
            </form>
          </td>
        </tr>
      <% }) %>
    </tbody>
  </table>
</body>
</html> 