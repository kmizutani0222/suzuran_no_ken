<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title>管理 - スキル効果一覧</title>
</head>
<body>
  <h1>スキル効果一覧</h1>
  <p><a href="/skill-effects/new">+ 新規登録</a></p>
  <table border="1" cellpadding="6">
    <thead>
      <tr>
        <th>名前</th>
        <th>カテゴリー</th>
        <th>効果説明</th>
        <th>操作</th>
      </tr>
    </thead>
    <tbody>
      <% if (items.length === 0) { %>
        <tr><td colspan="4">データがありません</td></tr>
      <% } %>
      <% items.forEach(i => { %>
        <tr>
          <td><%= i.name %></td>
          <td><%= i.category %></td>
          <td><pre><%= i.description %></pre></td>
          <td>
            <a href="/skill-effects/<%= i.id %>/edit">編集</a>
            <form action="/skill-effects/<%= i.id %>/delete" method="post" style="display:inline;">
              <button type="submit" onclick="return confirm('削除しますか？')">削除</button>
            </form>
          </td>
        </tr>
      <% }) %>
    </tbody>
  </table>
</body>
</html> 